<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_send_product_info_wizard_form" model="ir.ui.view">
        <field name="name">send.product.info.wizard.form</field>
        <field name="model">send.product.info.wizard</field>
        <field name="arch" type="xml">
            <form string="Send Product Information">
                <group>
                    <field name="partner_ids" widget="many2many_tags"/>
                    <field name="subject"/>
                    <field name="body"/>
                    <field name="attachment_id" invisible="1"/>
                </group>
                <footer>
                    <button name="action_send_mail" string="Send" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_send_product_info_wizard" model="ir.actions.act_window">
        <field name="name">Send Product Information</field>
        <field name="res_model">send.product.info.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>